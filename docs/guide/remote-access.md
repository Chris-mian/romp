# Self-hosted and remote access

You host Romp yourself and reach it from wherever you are, with no third-party
service in between.

<!-- TODO: expand. Cover:
     - the local kernel and the browser dashboard on 127.0.0.1
     - the VS Code / Cursor extension as a second surface on the same kernel
     - federating several machines over SSH into one fleet, with cross-machine messaging
     - reaching the dashboard from a phone over Tailscale (romp --serve)
     - what leaves your machine (only the claude CLI) and what never does
-->
