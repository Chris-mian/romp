# Keeping the fleet moving

An agent that goes idle with open work gets nudged back to it, so progress does
not wait on you noticing a stall.

<!-- TODO: expand. Cover:
     - what counts as "stalled with open work"
     - what a nudge does and when it fires
     - how to turn nudging off for a session
     - the mechanism lives in design/stalled-open-todos-nudge.md
-->
