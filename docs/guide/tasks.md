# Tasks, not transcripts

Romp organizes what the agents do into human-facing tasks, so you follow the
work by outcome instead of scrolling transcripts.

<!-- TODO: expand. Cover:
     - how tasks are inferred from live sessions
     - tasks that interleave within one session, and tasks that span several
     - what a task card shows: owner, handoffs, what is done, what needs you, summary, background
     - picking a thread back up after time away
-->
